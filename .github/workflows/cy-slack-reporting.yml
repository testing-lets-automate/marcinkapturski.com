name: Slack notification

on:
  workflow_call:

jobs:
  report:
    name: Report results on Slack
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      # - name: Slack notification
      #   if: always()
      #   run: |
      #     export SCRIPT_PATH=${{ github.workspace }}/cypress/slack/message.js
      #     yarn node $SCRIPT_PATH

      - name: Slack notification
        if: always()
        shell: bash
        run: |
          echo ::set-output name=XRAY_TOKEN::$(curl -X POST -H 'Content-type: application/json' --data '{"text":"Hello, World!"}' https://hooks.slack.com/services/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| tr -d '"')
